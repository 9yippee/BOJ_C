#include <stdio.h>
#pragma warning (disable:4996)

int main()
{
    int a, x, y;
    scanf("%d", &a);
    for (int i = 0; i < a; i++)
    {
        scanf("%d %d", &x, &y);
        
    }

    return 0;
}